name: Windows CMake Build CI

on:
  push:
    branches: [ "main", "master" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: windows-latest

    env:
      BUILD_CONFIG: ${{ startsWith(github.ref, 'refs/tags/') && 'Release' || 'Debug' }}

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Configure CMake
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{ env.BUILD_CONFIG }}
      
    - name: Build Server GUI
      run: cmake --build ${{github.workspace}}/build --config ${{ env.BUILD_CONFIG }} --target myservergui

    - name: Build Client GUI
      run: cmake --build ${{github.workspace}}/build --config ${{ env.BUILD_CONFIG }} --target myclientgui

    # -------------------------------------------------------------
    # FILE RENAMING (Only runs on normal branch pushes)
    # -------------------------------------------------------------
    - name: Rename Files with Debug Suffix
      if: ${{ env.BUILD_CONFIG == 'Debug' }}
      shell: pwsh
      run: |
        Move-Item -Path "${{github.workspace}}/build/bin/Debug/myclientgui.exe" -Destination "${{github.workspace}}/build/bin/Debug/clientGUI_debug.exe" -Force
        Move-Item -Path "${{github.workspace}}/build/bin/Debug/myservergui.exe" -Destination "${{github.workspace}}/build/bin/Debug/serverGUI_debug.exe" -Force
        
    # -------------------------------------------------------------
    # FILE RENAMING (Only runs on normal branch pushes)
    # -------------------------------------------------------------
    - name: Rename Files with Debug Suffix
      if: ${{ env.BUILD_CONFIG == 'Release' }}
      shell: pwsh
      run: |
        Move-Item -Path "${{github.workspace}}/build/bin/Release/myclientgui.exe" -Destination "${{github.workspace}}/build/bin/Release/clientGUI.exe" -Force
        Move-Item -Path "${{github.workspace}}/build/bin/Release/myservergui.exe" -Destination "${{github.workspace}}/build/bin/Release/serverGUI.exe" -Force

    # -------------------------------------------------------------
    # RELEASES SECTOR
    # -------------------------------------------------------------

    - name: Create Rolling Release (On Every Push - DEBUG BUILD)
      if: ${{ env.BUILD_CONFIG == 'Debug' }}
      uses: softprops/action-gh-release@v2
      with:
        tag_name: latest
        name: Latest Development Build (Debug)
        prerelease: true
        files: |
          ${{github.workspace}}/build/bin/Debug/serverGUI_debug.exe
          ${{github.workspace}}/build/bin/Debug/clientGUI_debug.exe

    - name: Create Official Version Release (On Explicit Tag - RELEASE BUILD)
      if: ${{ env.BUILD_CONFIG == 'Release' }}
      uses: softprops/action-gh-release@v2
      with:
        files: |
          ${{github.workspace}}/build/bin/Release/serverGUI.exe
          ${{github.workspace}}/build/bin/Release/clientGUI.exe